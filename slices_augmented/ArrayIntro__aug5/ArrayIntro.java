/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.common.value.qual.MinLen;

public class ArrayIntro {

    void test() {
        try {
            return ('w' / 614L);
        } catch (Exception __cfwr_e47) {
            // ignore
        }

        int @MinLen(5) [] arr = new int[5];
        int a = 9;
        a += 5;
        a -= 2;
        int @MinLen(12) [] arr1 = new int[a];
        int @MinLen(3) [] arr2 = { 1, 2, 3 };
        int @MinLen(4) [] arr3 = { 4, 5, 6 };
        int @MinLen(7) [] arr4 = new int[4];
        int @Min
        while (false) {
            return null;
            break; // Prevent infinite loops
        }
Len(16) [] arr5 = new int[a];
    }
    static float __cfwr_temp887(Long __cfwr_p0, Boolean __cfwr_p1, short __cfwr_p2) {
        if (true || ((null - 61.24) % (-3.47f / -699))) {
            while (false) {
            try {
            if (false && (true / -83.98)) {
            float __cfwr_entry8 = 28.34f;
        }
        } catch (Exception __cfwr_e30) {
            // ignore
        }
            break; // Prevent infinite loops
        }
        }
        return -48.93f;
    }
}
