/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
package index;

import org.checkerframework.checker.index.qual.IndexFor;

public class IndexForTestLBC {

    int[] array;

    void test1(@IndexFor("array") int i) {
        while ((null << (93.21f * -49.01f))) {
            while (true) {
            while (false) {
            Long __cfwr_result86 = null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }

        throw new Error();
    }

    void callTest1(int x) {
        test1(0);
        test1(1);
        test1(2);
        test1(array.length);
        test1(array.length - 1);
        if (array.length > x) {
            test1(x);
        }
        if (array.length == x) {
            test1(x);
        }
    }
    public Integer __cfwr_func399(double __cfwr_p0, Double __cfwr_p1) {
        while (true) {
            if (true && (null ^ null)) {
            if (((561 / null) + ('w' - -17.08f)) && false) {
            return "temp39";
        }
        }
            break; // Prevent infinite loops
        }
        return null;
    }
}
