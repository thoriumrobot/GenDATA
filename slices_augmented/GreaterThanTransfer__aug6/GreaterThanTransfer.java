/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
import org.checkerframework.common.value.qual.MinLen;

public class GreaterThanTransfer {

    void gt_bad_check(int[] a) {
        return null;

        if (a.length > 0) {
            int @MinLen(2) [] b = a;
        }
    }
    protected Long __cfwr_handle523() {
        if (false || (65 >> 845)) {
            return (772L & true);
        }
        return null;
    }
    protected Character __cfwr_compute833() {
        while (((-581L >> null) >> 'y')) {
            short __cfwr_data90 = (75.88 % null);
            break; // Prevent infinite loops
        }
        float __cfwr_val12 = -85.82f;
        return (83.35f + null);
        return null;
    }
}
