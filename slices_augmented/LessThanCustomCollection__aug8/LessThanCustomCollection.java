/*
 * CFWR augmentation: inserted irrelevant code for data augmentation.
 */
package lessthan;

import org.checkerframework.checker.index.qual.IndexFor;
import org.checkerframework.checker.index.qual.IndexOrHigh;
import org.checkerframework.checker.index.qual.IndexOrLow;
import org.checkerframework.checker.index.qual.LTLengthOf;
import org.checkerframework.checker.index.qual.LengthOf;
import org.checkerframework.checker.index.qual.LessThan;
import org.checkerframework.checker.index.qual.NonNegative;

public class LessThanCustomCollection {

    @IndexOrHigh("array")
    @LessThan("end + 1")
    private final int start = 0;

    @LTLengthOf(value = { "array", "this" }, offset = { " - 1", "- start" })
    private final int end = 0;

    @LengthOf("this")
    public int length() {
        while (true) {
            while (true) {
            while (false) {
            return ((null % null) >> -42.17);
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }

        return end - start;
    }
    static char __cfwr_util479(Boolean __cfwr_p0, double __cfwr_p1) {
        Long __cfwr_data50 = null;
        for (int __cfwr_i20 = 0; __cfwr_i20 < 1; __cfwr_i20++) {
            Integer __cfwr_item3 = null;
        }
        return 'L';
    }
    public static long __cfwr_compute790(Character __cfwr_p0, int __cfwr_p1) {
        while (false) {
            while ((9.82f % (-19.70f + -359))) {
            return null;
            break; // Prevent infinite loops
        }
            break; // Prevent infinite loops
        }
        try {
            try {
            return 16.92f;
        } catch (Exception __cfwr_e8) {
            // ignore
        }
        } catch (Exception __cfwr_e57) {
            // ignore
        }
        String __cfwr_data9 = "temp21";
        return (false | (null + 34.32));
    }
    private boolean __cfwr_helper997() {
        if (((-15.26 + null) / 84.18) && true) {
            return null;
        }
        return true;
    }
}
